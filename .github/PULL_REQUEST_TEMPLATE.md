## Summary

<!-- What does this PR change, and why? -->

## Test plan

- [ ] `npm run lint:js` / `npm run lint:css` pass
- [ ] `phpcs --standard=.phpcs.xml .` passes
- [ ] `npm run build` succeeds
- [ ] Manually tested in a local WordPress install (`npm run env:start`)

## Changelog entry

<!-- One line for readme.txt's Changelog section, if user-facing. -->
